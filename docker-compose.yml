version: '3.9'
services:
  orderq-admin:
    container_name: orderq-admin
    build: .
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev -- --host
